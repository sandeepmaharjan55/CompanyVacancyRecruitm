@model IEnumerable<FirstTask_Project.Models.Person>

@{
    ViewBag.Title = "Index";
}





<p>
    @Html.ActionLink("Create New", "Create")
</p>
<div class="pull-right">
    @using (Html.BeginForm("Index", "Person", FormMethod.Get))
    {
        @Html.TextBox("q", "", new { placeholder = "Search..." });

        <button type="submit" class="btn btn-primary">GO</button>

    }
</div>

<table class="table">
    <tr>
        <th>
            <a href="~/Person?order=name">
               Name
            </a>
        </th>
        <th>
            <a href="~/Person?order=gender">
                Gender
            </a>
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Contactno)
        </th>
        <th>
            <a href="~/Person?order=email">
                Email
            </a>
        </th>
        <th>
            <a href="~/Person?order=Exp">
                ExpYear
            </a>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Gender)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Contactno)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Experience.ExpYear)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", "Person", new { id=item.PersonId }, new { @class = "btn btn-default" }) |
            @Html.ActionLink("Details", "Details", "Person", new { id=item.PersonId }, new { @class = "btn btn-default" }) |
            @Html.ActionLink("Delete", "Delete", "Person", new { id=item.PersonId }, new { @class = "btn btn-default" })
        </td>
    </tr>
}

</table>
<p>@Html.ActionLink("Back to List", "Index","Person",new { @class = "btn btn-primary" })</p>
